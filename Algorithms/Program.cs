using Algorithms.Extensions;
using Algorithms.LeetCodeGoogle;

var problem = new StrobogrammaticNumber();

problem.FindStrobogrammatic(2).Display();

var linkedList = new LinkedList<char>();
linkedList.AddFirst(new LinkedListNode<char>('a'));
linkedList.AddFirst(new LinkedListNode<char>('b'));

linkedList.Display();


